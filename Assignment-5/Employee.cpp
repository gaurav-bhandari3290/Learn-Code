/*Class Employee {

    string name;
    
     int age;
    
     float salary;
    
    public : string getName();
    
    void setName(string name);
    
     int getAge();
    
    void setAge(int age);
    
    float getSalary();
    
    void setSalary(float salary);
    
     };
    
Employee employee;


Employee is a data structure because it only uses getters and setters. Data structures just hold and expose data, while objects also include logic. Since this class doesn’t do anything with the data, it behaves like a data structure, not a true object.
*/
